# pereira-sql
Biblioteca de manipulação SQL.

## Build notes

This project depends on proprietary SAS and Oracle Spatial libraries that are not distributed through Maven Central.

Before building on a new machine, install the local jars:

```bat
scripts\install-local-jars.bat